{
  "name": "wc2026-pickems",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "db:init:local": "wrangler d1 execute wc2026 --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute wc2026 --remote --file=schema.sql",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
