# Python
venv/
__pycache__/
*.pyc

# Node / Wrangler
node_modules/
.wrangler/
.dev.vars

# OS / editor
.DS_Store
Thumbs.db
server.log

# Local-only review folder (not deployed)
archive/
.claude/

# Private participant data (names/emails) — never commit
data/private/

# Local-only preview page (reads private board.json)
preview.html
data/board.json

# Preview-only results dump (never to prod)
data/_preview_results.json

# Generated email cards (carry names) — never commit
out/
